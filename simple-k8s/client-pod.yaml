apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
      component: web
spec:
  containers:
    - name: client
      image: ricardosouzamorais/multi-fib-client
      ports:
        - containerPort: 3000